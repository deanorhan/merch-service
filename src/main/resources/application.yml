app:
  security:
    role:
      admin: "ADMIN"
      fan: FAN
      vendor: "VENDOR"

spring:
  profiles:
    active: ${DEPLOY_ENV:default}
  application:
    name: merch-service
  jackson:
    serialization:
      fail-on-empty-beans: false
  jpa:
    open-in-view: false